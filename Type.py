country = str("Brazil")
age = 15
money = 28.5
bool = True

print(
    type(country),
    type(age),
    type(money),
    type(bool) )
# It's not completly necessary to put all the types on the variables
# but if in the future you will use that variable to another thing
# or if you will do a input, it's likeable that you define the type